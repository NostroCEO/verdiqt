# Project State

Read this before doing anything. Update it at the end of every working session: move items between sections, date them, keep decisions immutable (append corrections, never silently rewrite history). This file is how any coding agent resumes exactly where the project stands.

Last updated: 2026-08-29. Engineering review complete; 7 accepted fixes shipped (security, integrity, performance, reliability). Core pipeline, courtroom UI, live SSE, and all 12 WebMCP tools are deployed and functional on Render.

## Where we are

- [x] 2026-08-26: Product idea and founder intent locked in the supplied Verdiqt documents.
- [x] 2026-08-26: Documentation normalized into the canonical root and `docs/` layout, with requirements, contracts, rules, resources, demo, and agent instructions reconciled.
- [x] 2026-08-26: Devpost registration confirmed by the founder; official dates, criteria, resources, and submission requirements refreshed from the live challenge data.
- [x] 2026-08-26: Task 1 scaffold created with Next.js 15.5.24, React 19.2.8, exact dependencies, shadcn 4.19.0 components, Verdiqt tokens, and the first branded landing shell.
- [x] 2026-08-26: The landing shell passed lint, TypeScript, production build, tests with no test files allowed for Task 1, and Playwright visual inspection at 1440 x 1000 and 375 x 812 with no horizontal overflow.
- [x] 2026-08-26: The founder rejected the soft glow and glass landing direction. The replacement uses a hard-edged editorial hero, original CSS halftone gavel, repository-first local preview, official challenge supporter attribution, interactive proceeding, and a light procedural-record section.
- [x] 2026-08-26: The editorial landing passed Playwright inspection at 1440 x 1000 and 375 x 812, all three walkthrough stages, keyboard-only intake, visible focus, element-bound and horizontal-overflow checks, and a reduced-motion run with zero active animations. Hydration and runtime consoles are clean.
- [x] Local git repository initialized on `main` with the first project commits. No remote is configured yet.
- [x] Application code: Task 1 passed the frozen-lockfile and pinned Node 22 verification. The next engineering action is Task 2 deployment, then the backend sequence.
- [x] 2026-08-27: Task 2 local foundation added a schema-valid Render web and Postgres preview plus `GET /api/health`. The pinned production server returned both `{ "ok": true, "sha": "dev" }` and an injected revision. Lint, typecheck, build, tests, and frozen install are green.
- [x] 2026-08-27: The public landing now omits event and protocol naming, uses four official transparent selected-technology marks, and contains no supporter-mark claims or long trademark paragraph.
- [x] 2026-08-27: The duplicate six-feature dossier was removed. The proceeding is one compact persistent product frame with three 40 px controls and 4.8-second stages. A valid GitHub URL scrolls to the preview, carries its repository name into the frame, and forces one complete three-stage run.
- [x] 2026-08-27: The refreshed landing passed lint, TypeScript, production build, tests with no test files allowed, and live Playwright checks. At 1440 px the proceeding is 845 px tall with a 520 px frame and exact 40 px controls. At 375 x 812 it is 689 px tall with a 400 px frame, exact 40 px controls, no clipped visible text, and zero horizontal overflow. The CTA reached all three stages, keyboard focus paused the timer, reduced motion stayed on stage one with zero active animations, all four SVGs returned 200, and the console had zero errors or warnings.
- [x] 2026-08-27: Official Devpost resources and current WebMCP primary documents were refreshed. The current baseline is `document.modelContext`, abortable tool registration cleanup, direct serializable results, and explicit authoritative status reads instead of the removed page-context publishing API.
- [x] 2026-08-27: Local backend foundation implemented ahead of the live gate with the gate honored (nothing deploys, launch flags stay false): Task 3 complete (schema, init migration with CHECK constraints and owner trigger, db clients), Task 4 partial (queue with ensured run-trial queue and explicit dedupe, idempotent event emitter, anonymous capability access), Task 11 partial (POST /api/trials launch-gated, contract-shaped GET status), Task 12 partial (12-tool registry on document.modelContext, provider with AbortSignal cleanup). A four-auditor conformance review against docs/ARCHITECTURE.md found and fixed: silent pg-boss job drop (queue never created), non-idempotent emitEvent, status-route contract drift (stages_done/latest_events/approval mapping, payload whitelisting), missing untrustedContentHint on list_repos, and a cold-start poisoned queue singleton. Lint, typecheck, build, and 32 tests green; em dash count zero.
- [x] 2026-08-27: GitHub half of the Task 2 live gate closed. Public repository https://github.com/NostroCEO/verdiqt created under the founder's NostroCEO account (device-flow auth added as a second gh account scoped to this repo via a repo-local credential helper; GR-AYLE stays the machine's active identity), full main history pushed with in-window timestamps, MIT LICENSE at root, footer Source link activated.
- [x] 2026-08-27: Task 2 live gate CLOSED. Founder claimed the participant credits and applied the Blueprint "verdiqt"; live base URL is https://verdiqt-web.onrender.com. Verified: `GET /api/health` returns `{ ok: true, sha: "fe05687..." }` exactly matching the pushed HEAD, and both `/` and `/trial` serve 200. Every push to main now auto-deploys. The preview remains health-plus-static only: launch flags stay false (Gate A) and no OpenAI key is set (Gate B). Worker service joins render.yaml at the founder-approved paid gate; until then the worker runs locally.

## Decision log (immutable)

| Date | Decision | Why |
|---|---|---|
| 2026-08-26 | Product: Verdiqt, a validation copilot where human and agent judge SaaS ideas together | Strongest fit for the hackathon theme and the founder's anti-token-burn mission |
| 2026-08-26 | Collaborative cockpit over one-way tools | Two-way loop (context + approvals) is the WebMCP Leverage play |
| 2026-08-26 | Evidence: live APIs + curated RAG brain; NO site scraping | Rules-safe, buildable in 8 days, credible citations |
| 2026-08-26 | Verdict: 6 weighted dimensions, 0 to 100, BUILD/PIVOT/KILL at 70/40, one next step | Programmatic, comparable, agent-actionable |
| 2026-08-26 | Hosting: everything on Render (web, worker, cron, Postgres+pgvector) | No serverless timeouts, one dashboard; rules allow any host |
| 2026-08-26 | Data: Prisma + pooling, pg-boss queue in Postgres, ApiCache table | One datastore, founder requirement for pooling + cache |
| 2026-08-26 | UI: shadcn/Tailwind + Motion + KokonutUI + Bklit charts + anime.js/Three hero | Founder-selected stack (uitools.md); Bklit owns ALL charts |
| 2026-08-26 | Agent presence: popping dock, never a permanent panel | Founder requirement: framed motion, expandable, closable |
| 2026-08-26 | GitHub OAuth portfolio sweep under account NostroCEO; public-repo-URL path kept for zero-friction judging | Founder requirement + judge accessibility |
| 2026-08-26 | Priorities: core flow > polish > portfolio OAuth > cron monitoring | Founder cut line |
| 2026-08-26 | Em dash banned in all UI copy; docs follow the same rule | Founder requirement |
| 2026-08-26 | Submit September 2, one day early | Deadline risk management |
| 2026-08-26 | Canonical planning files live under `docs/`; `CODEX_PROMPT.md` is the agent kickoff contract; Next.js uses no `src/` directory | Removes duplicate scaffold content and makes every local link resolve |
| 2026-08-26 | Trial access uses server-validated ownership capabilities; approvals and idempotency are persistent data, not client-only state | Prevents cross-session access, replayed writes, and approval bypasses |
| 2026-08-26 | Superseded in the same session: Devpost judging percentages were initially not assumed | The criteria endpoint omitted weights, but the later full-rules check found the controlling language in section 7 |
| 2026-08-26 | Correction: the previously referenced `docs/superpowers/specs/2026-08-26-verdiqt-design.md` is not present in the supplied project | The canonical design source is `docs/UI_DESIGN.md` plus `uitools.md` unless the founder later supplies the archived spec |
| 2026-08-26 | The four Devpost criteria are equally weighted, with ties resolved in listed order starting with WebMCP Leverage | Official rules section 7 controls when the shorter criteria endpoint omits weighting metadata |
| 2026-08-26 | Correction to hosting scope: Render remains the primary host for web, worker, and Postgres; cron is optional Task 18. Vercel and Netlify accounts are available only as contingency web hosts. No topology switch occurs without founder approval and end-to-end verification. | Render supports the required long-running pg-boss worker; account availability alone does not prove topology compatibility |
| 2026-08-26 | Task 1 pins Node 22.23.2, pnpm 11.24.0, Next.js and create-next-app 15.5.24, React 19.2.8, shadcn 4.19.0, and the exact dependency graph in package.json and pnpm-lock.yaml. | Current official package metadata was checked before installation; OpenAI v5 requires Zod 3, so Zod 3.25.76 is pinned |
| 2026-08-26 | The current shadcn CLI uses the `base-nova` preset rather than the legacy style prompt; Task 1 uses its neutral CSS-variable configuration and then applies Verdiqt tokens. | This is the exact current CLI behavior and keeps the generated component contracts current |
| 2026-08-26 | Every UI-affecting change requires Playwright visual validation at the affected desktop viewport and 375 px, including screenshots and horizontal-overflow inspection. | Founder established this as a permanent quality rule after the first landing shell was built |
| 2026-08-26 | Correction to the earlier landing animation ownership: Motion owns the landing DOM hero, intake, and walkthrough. anime.js and Three.js are optional for one later isolated 3D moment only. | The founder explicitly requested motion.dev animation; the current original CSS halftone experience is complete, accessible, and avoids an unnecessary WebGL dependency |
| 2026-08-26 | Landing visual language is hard-edged editorial: flat near-black fields, 1280 px rail shell, square modules, one-pixel borders, mono protocol labels, original halftone courtroom art, and a light procedural-record section. | The founder rejected the generic AI glow, glass, and full-page grid direction after seeing the first render |
| 2026-08-26 | TesterArmy is a runtime layout and pacing reference only. Verdiqt does not copy its source, exact copy, SVG paths, skull art, customer marks, testimonials, or brand assets. | Preserves an original product identity and source compliance while using public computed layout and interaction patterns as inspiration |
| 2026-08-26 | The landing lists OpenAI, Cloudflare, Vercel, Shopify, Google Chrome, Render, and Netlify as text-only WebMCP Challenge supporters with a visible non-endorsement disclosure. | These are the names shown in the official challenge resource hub; text-only factual attribution avoids implying a direct Verdiqt relationship or using unverified logo rights |
| 2026-08-27 | The Task 2 Blueprint provisions only the web service and Postgres preview; Task 4 adds the background worker. | Render's current Blueprint schema requires at least one instance and has no suspended-service field, while workers have no free plan and the worker entrypoint does not exist before Task 4 |
| 2026-08-27 | The checked-in preview defaults to Frankfurt, PostgreSQL 17, free web and Postgres plans, and disabled public trial and GitHub OAuth flags. It uses direct database connections until the founder approves a paid PgBouncer-enabled database before public workloads. | This keeps the health-only preview no-cost and fail-closed without silently making a spending decision; region, version, and plan must be confirmed before applying the Blueprint |
| 2026-08-27 | Correction to the earlier public supporter matrix: the landing shows only official transparent marks for OpenAI, Next.js, Prisma, and Render as selected implementation technologies. Cloudflare, Shopify, Chrome, Netlify, Vercel, and other event-supporter marks remain absent. | The live resource hub proves event participation but grants no blanket logo-reuse permission. Unintegrated or restricted marks could imply a product relationship that does not exist. |
| 2026-08-27 | Public marketing copy omits the event name and browser-tool protocol name; engineering, rules, and submission documents retain both where technically required. | The founder requested a product-first public surface without event framing. Internal accuracy and judging proof still require the real terms. |
| 2026-08-27 | Correction to the landing story: the six-feature dossier is rejected. One compact three-stage product frame now carries the full explanation. | The second long feature sequence duplicated the proceeding, forced unnecessary scrolling, and made the core flow harder to understand. |
| 2026-08-27 | A valid landing submission currently starts only an explicitly labeled local product preview, then scrolls to and runs its three visual stages. It does not claim a server trial or force an external agent tool call. | The trial API and durable pipeline do not exist yet. When Task 11 is complete, the human CTA and `start_validation` will share `POST /api/trials`; browser tool discovery remains available from root-provider registration rather than being created by a click. |
| 2026-08-27 | Correction to the browser-tool contract: `document.modelContext` is primary, registration is cleaned up through `AbortSignal`, tool callbacks return direct serializable values, and agent-visible human changes come from authoritative status reads. `navigator.modelContext` is legacy-only after live proof. | Current official specification and Chrome imperative API removed the planned context-publication method and define the document surface and abortable lifecycle. |
| 2026-08-27 | Bklit Gate 1 is closed for `@bklit/gauge-chart` and `@bklit/radar-chart` through the configured `https://ui.bklit.com/r/{name}.json` registry. Section 2 may use both before Task 15 as the dashboard visual proof. | The founder explicitly prioritized a real dashboard preview. Current Bklit documentation confirms these exact registry IDs, composable APIs, token theming, responsive sizing, and Motion-based enter animation. |
| 2026-08-27 | Correction: Section 2 is the real shared Verdiqt trial workspace, not a sandbox or tutorial carousel. Before the backend exists, only Phase 1 is active; Phases 2 and 3 stay locked. After Task 11, only persisted pipeline state and SSE events may advance it, and Bklit charts render only from a real completed verdict. | The temporary sandbox validator, looping phase animation, and fabricated readiness contradicted the product contract and the evidence-first principle. |
| 2026-08-27 | Correction: the landing trial surface is an embedded interactive dashboard, not a narrative section. It contains no Motion. Phase 1 stays selected before and after local intake; only the real trial API and persisted pipeline state may advance it. | The founder wants dashboard behavior and direct system visibility, not a staged landing animation. |

| 2026-08-27 | Engineering notes bound to later tasks: the sign-in transfer transaction must UPDATE Trial before Approval (owner-copy trigger ordering); AnonymousSession expiry cleanup must re-own or delete trials before deleting sessions (SET NULL FKs vs the exactly-one-owner check); DIRECT_DATABASE_URL is required in production for pg-boss (throws) and db.ts warns on fallback; the sandbox status row satisfies Task 12's landing agent-banner requirement (banner scope reduced to product screens); the client tool event bus lives at lib/webmcp/bus.ts and never carries SSE worker events. | Conformance audit of 2026-08-27; prevents known-ordering and misrouting failures from resurfacing in Tasks 4, 11, 12, and 17 |

| 2026-08-27 | Gate A CLOSED with founder-approved defaults: stored data is idea text or repo URL, gathered public evidence snippets, scores, the event log, and the anonymous session hash only; anonymous trials auto-delete 30 days after completion; deletion requests honored by email; GitHub OAuth stores only the public login and a revocable token, unlink revokes it; a /privacy page stating all of this ships with Task 11 and PUBLIC_TRIALS_ENABLED flips only after it is live; anonymous trial creation stays rate limited (5/day/IP) with the judge code bypassing only that limit. | Founder decision D19, 2026-08-27 |
| 2026-08-27 | Gate B SUPERSEDED by the zero-budget decision: no paid OpenAI use anywhere. Inference runs on a free-tier cloud provider through an OpenAI-compatible endpoint (exact provider, model IDs, and current free-tier terms verified live and recorded in SOURCE_COMPLIANCE before the first call; hard caps keep usage inside free quotas). The WEB_SEARCH evidence source is disabled with the typed source_disabled response. Knowledge-brain retrieval uses Postgres full-text search instead of paid embeddings; the vector columns stay in the schema, unused, for a future upgrade. Subscription-CLI piping (ChatGPT/Claude consumer plans as an API) is rejected as a terms violation. | Founder decision D20: $0 hackathon budget; Devpost requires authorized third-party use |

| 2026-08-28 | Motion amendment: the live courtroom dashboard REQUIRES state-driven Motion (motion.dev patterns: keyed AnimatePresence entrances, spring pane switches, layoutId underline). Decorative, autoplay, and fabricated animation remain banned; every animated element renders real persisted state. Amends the 2026-08-27 static-chrome rule for the live workspace only. Additional founder UX rules: completed phases render as completed; locks exist only before a run starts; sources are visible during research; the active case never appears in its own archive (archiving begins when a new case starts). | Founder direction after the first live trial, 2026-08-28 |

| 2026-08-28 | Live-research amendment (supersedes the Reddit gate and the WEB_SEARCH cut): the founder directed at-trial-time research through public free APIs — Reddit public JSON search (honest client identity, 12h cache, failures surface as source_failed) and Stack Overflow's public search API in the WEB_SEARCH slot. No vector embeddings: pgvector stays dormant (Groq has no embeddings endpoint; local models risk OOM in the 512MB container). Corpus grounding stays Postgres full-text, fixed to OR-semantics with a dimension-tag backfill so every scoring call receives passages, with a knowledge_retrieved event per dimension. crawl4ai-style headless-browser scraping is rejected for the free container. | Founder decisions, 2026-08-28: "we give the Agents what they need … through reddit and others", "harness engineering", zero budget |
| 2026-08-28 | UX amendments: scores wear their meaning everywhere (>=70 green/build, 40-69 orange/pivot, <40 red/kill) and the verdict headline is colored the same way; official brand marks (GitHub, Hacker News, Reddit, Product Hunt, Stack Overflow) replace lucide stand-ins in the research feed; the running stage and live evidence feed pulse (state-driven only); the public rate-limit message never mentions /judge (that copy was judge-facing; judges get the code privately in the submission packet). Default daily limit is 15 in code, no env needed. | Founder direction, 2026-08-28 |
| 2026-08-28 | Capacity and retention hardening: Prisma pools capped (6 web + 3 direct) and pg-boss at 4 against the ~95-connection free Postgres; the duplicate colocated worker spawn removed (instrumentation owns the worker); poll routes carry 2s in-process micro-caches and a 60s principal cache; session sliding expiry rewrites at most every 6h; a twice-daily retention sweep deletes expired ApiCache rows, stale rate-limit rows, and trials of sessions expired more than 7 days (cascade), keeping archives permanent for any owner who can still reach them while the DB never grows unbounded. | Founder demand "archives must not make the DB heavy", 2026-08-28 |

## Open questions (resolve with the founder, not unilaterally)

- Exact production domain (Render subdomain vs custom domain).
- Confirm the Task 2 provisioning defaults before applying the Blueprint: Frankfurt, PostgreSQL 17, and free preview plans. Approve a paid database with managed PgBouncer before public trial traffic, and approve the paid worker plan when Task 4 is ready.
- Gate B: exact OpenAI model IDs for normalization/scoring, web search, deep scan/composition, and 1536-dimension embeddings; per-trial call and evidence caps; timeouts and retry bounds; and development and demo spend ceilings.
- Demo idea for the video is "AI changelog writer for indie SaaS teams" per docs/DEMO_SCRIPT.md; confirm before recording.
- Data-retention period and account-deletion behavior. Requirements leave both unspecified until the founder decides.
- Whether to enable the optional direct Reddit adapter. It requires registered Data API OAuth credentials, accurate client identity, and a retention policy approved against the current terms; it remains disabled until all are resolved.
- Exact KokonutUI registry identifiers and display-serif choice. Bklit gauge and radar identifiers are now verified. anime.js and Three.js APIs need verification only if the founder later requests the optional isolated 3D moment.
- Gate A: collected data categories, retention periods, anonymous and signed-in deletion behavior, GitHub unlink/token revocation behavior, and the public privacy notice URL. Public trials and GitHub OAuth remain disabled until resolved.

The `.devpost-hackathon-state.json` fields `next_command` and `submission.draft_file` are owned by the Devpost plugin workflow. They intentionally do not define the engineering next action or canonical documentation layout. For engineering, this file and `docs/PLAN.md` control; the next action is the Task 2 GitHub and Render human gate, followed by live health verification. If the plugin prepares a root `devpost-submission.md`, integrate the accepted draft into `docs/submission/` without changing plugin state behind its back.

- 2026-08-27: Founder-directed page split: the landing (/) is presentation only ([01] hero, [02] procedural record, [03] THE COURTROOM mid-page CTA, selected stack, [05] closing band); the working dashboard and the [02] agent-access sandbox moved to the new /trial page ("Verdiqt | The courtroom"). The hero intake validates locally and navigates to /trial?case=&source=&type= where the dashboard opens Phase 1 with the case (URL params via a Suspense-wrapped client bridge; still local preview state, no server-trial claim). Shared SiteHeader/SiteFooter extracted; the dashboard's add-URL control navigates home when no intake is on-page. The technology wall no longer has ANY hidden pre-state: all four marks render in every static capture at every scroll position (the earlier reveal stagger was dropped after a full-page capture reproduced the blank-band failure mode). Verified: build (both routes static), 40 tests, both pages at 1440x1000 and 375x812 with zero overflow and clean consoles, and the full intake-to-courtroom click-through. Reference check: the official Cubecade WebMCP example follows the same ambient-live-state pattern as our sandbox.

- 2026-08-27: Added the courtroom filing desk: /trial carries its own compact intake (FILE A CASE: repo/idea tabs, input, CTA) above the dashboard, sharing extracted validation helpers (lib/intake.ts) with the hero. Filing dispatches the in-page preview event so Phase 1 loads without navigation, and mirrors the case into /trial?case=&source=&type= for shareability; the dashboard's add-URL control now scrolls to whichever page's intake hosts it. Verified: typecheck, 40 tests, build, in-place filing flow at 1440 and clean 375 stack.

- 2026-08-27: Render credits claimed by the founder; Blueprint application pending (verdiqt-web.onrender.com not yet serving). Implemented the Task 4 remainder: lib/events gained the (createdAt,id) tuple cursor query and terminal-kind helpers; lib/trial-stream.ts is the dependency-injected SSE core (id-cursor frames for gapless Last-Event-ID resume, 1500ms poll, 15s heartbeat, closes after flushing a terminal event); GET /api/trials/[id]/stream is owner-authorized with the identical 404 for unknown and foreign ids; worker/index.ts starts pg-boss and consumes run-trial with a conditional QUEUED-to-RUNNING claim (duplicate deliveries exit safely); worker/pipeline.ts is the Task 4 staged stub (NORMALIZING then COMPLETE at compositeScore 50, dedupe-keyed events, FAILED path marks run and trial and rethrows for retry). Task 11 replaces the stub body; the claim/idempotency contract is permanent. package.json test script switched to plain vitest run per Task 1. Gates: typecheck, 51 tests, production build, zero em dashes, pushed to origin.

- 2026-08-27 (evening): Zero-budget engine built ahead of schedule. Gate A closed (founder defaults); Gate B superseded by founder D20 ($0: Groq free tier, openai/gpt-oss-120b via OpenAI-compatible endpoint, terms verified and recorded in SOURCE_COMPLIANCE; web-search source cut with typed source_disabled; brain retrieval via Postgres full-text, vector columns dormant). Shipped: Task 6 evidence adapters (HN, GitHub, Product Hunt token-gated, web-search and Reddit disabled) with gatherAll resilience; Task 7 normalization (repo brief + sanitized README, structured NormalizedIdea) and the inference wrapper (JSON mode, zod, one repair retry, caps 20 calls / 48 items per trial); Task 9 corpus (12 original files written by a 12-agent workflow, zero em dashes, dimension-only tags) + idempotent ingest + full-text retrieveKnowledge + rate-limited GET /api/knowledge/search; lib/ratelimit (atomic ipHash+day upsert); Task 8 classification (25-item batches, explicit reduced-coverage events, all-failed throws); Task 10 weights/compose/score (thresholds at 70/40, pivot templates from the strongest dimension, weighted-deficit next-step selection honoring the catalog's ship_it and pain-before-pricing rules, code-enforced under-2-items 45 cap and citation validation with one corrective retry). 90 tests passing; pushed and auto-deployed. Awaiting the founder's Groq API key; next: Task 11 (real pipeline stages + full trial API + frontend wiring + /privacy page).

- 2026-08-28: Task 11 shipped end to end. render.yaml gained the verdiqt-worker background worker (paid from the founder's participant credits, zero cash; start runs the idempotent migrate:deploy) and PUBLIC_TRIALS_ENABLED flipped true alongside the new /privacy page (Gate A text). Server: the worker pipeline is the real five stages with revision superseding and dedupe-keyed events; new routes for evidence (filters), verdict (409 not_complete), next-step, refine (linked pivot trials), compare (2-5 owner trials + strongest-per-dimension), evidence PATCH and weights PUT (both open revisioned RESCORE runs), deep-scan-requests (persistent PENDING approvals, deduped per owner/revision/dimension), judge-access (form-only constant-time code exchange for the signed limit-bypass cookie) and the rate-limited POST /api/trials (429 rate_limited with judge bypass). Frontend: both intakes call the real API first (202 live / 503 honest local fallback / 429 judge hint), and the dashboard has a live mode driven by persisted state + SSE (phases unlock by status, metrics fill, connections rail reports Trial API/Postgres/Worker truthfully, evidence and verdict cells unlock only from real data). 91 tests, build green, all four pages serve, fallback click-through browser-verified. Founder actions pending: approve the worker service in Render's Blueprint sync and provide the Groq (with a q, console.groq.com, free tier) API key; first live end-to-end trial follows immediately.

- 2026-08-28: Founder manually set PUBLIC_TRIALS_ENABLED=true on verdiqt-web (dashboard env edit; Blueprint sync had not surfaced). The verdiqt-worker service still needs its Blueprint approval plus the INFERENCE_API_KEY secret; this commit exists partly to trigger a fresh Blueprint evaluation so the dashboard offers the new worker resource for approval.

- 2026-08-28 (later): Narrated live courtroom shipped after a focused eng review (6 findings folded, logged clean). The P1 fix: live state no longer depends on SSE delivery; status polls every 5s while non-terminal, evidence polls every 2.5s during research phases (visibility-paused), and workerSeen derives from any progress signal, so a working pipeline can never look dead again. UX per founder standards: clickable phase tabs once live with done phases checked (locks only pre-start), pane switcher (intake summary, research pane with the four-stage checklist and the live source feed showing where evidence comes from, verdict pane with the Bklit charts and the per-dimension rationale accordion plus File Another Case), the past-cases archive rail (active case excluded; archiving begins when a new case starts; GET /api/trials lists the session's trials), and motion.dev-verified animations (keyed AnimatePresence, springs with visualDuration, layoutId underline). trial-demo decomposed into components/trial/ panes. 97 tests, build exit-checked, pushed. Judge-path envs (AUTH_SECRET, JUDGE_ACCESS_CODE) still pending with the founder.

## Session journal (append one line per session)

- 2026-08-26: Brainstorm, spec, and full documentation kit. No code yet. Next: PLAN Task 1 scaffold, followed by the first deploy in Task 2.
- 2026-08-26: Read every supplied file; removed merged generic scaffold content; adopted `CODEX_PROMPT.md`; normalized paths and names; reconciled product, security, data, WebMCP, challenge, UI, and demo contracts. Next: initialize git, commit the baseline, then PLAN Task 1.
- 2026-08-26: Started the real build. Created the pinned Next.js and shadcn foundation, applied Verdiqt courtroom tokens, built the first landing shell, used live Devpost resources to retain the official WebMCP implementation path, and visually inspected desktop and 375 px layouts in Playwright. Next: exact Node 22 and frozen-lockfile verification, Git commits, then Task 2 deployment.
- 2026-08-26: Rebuilt the landing after founder review. Inspected tester.army public runtime geometry, transitions, animations, and SVG inventory without copying source or assets; replaced the soft AI visual language with an original editorial courtroom; added the seven official challenge supporters; verified desktop, mobile, keyboard, all walkthrough states, overflow, reduced motion, and clean hydration. Next: final frozen install, production build, commits, then Task 2 deployment before backend work.
- 2026-08-27: Implemented and locally verified Task 2 Steps 1 and 2. Added the official-schema-valid Render web and Postgres preview, kept launch gates false, exercised the health route with fallback and injected SHAs through `next start`, and corrected the worker staging plan to match Render's platform. Next: founder creates the GitHub remote and Render Blueprint, then we verify the live revision before Task 3.
- 2026-08-27 (second session): Ran the full review pipeline (CEO selective-expansion, design with outside voice, engineering with a four-auditor conformance workflow) on the landing refresh plan, fixed the audited backend defects (silent pg-boss queue drop, non-idempotent emitEvent, status-route contract drift), and committed the backend foundation in clean commits. Rebuilt the landing: D13 section order (hero, procedural record, proceeding, agent access, slim stack, closing band), the [04] AGENT ACCESS sandbox rendering the real 12-tool registry with allSettled partial-truth registration, bounded late-injection detection with passive re-checks, and a payload-free activity feed; dashboard agent-channel panel now mirrors the real store. Typography moved to reference metrics with legally clean faces (Geist display/body at 60px/500/68px, DM Mono 14px controls), settling the uitools.md Gate 4 question by founder decision; the reference's proprietary display font was not copied. Hero gained the founder-requested finite gavel tap (base stays static). OG image via next/og, original icon.svg, MIT footer with gated repo slot. Verified: production build, lint, typecheck, 40 tests, zero em dashes, Playwright at 1440x1000 and 375x812 (no overflow, no hidden-resting elements, one-line 44px tabs, 44px docket rows, clean console). Browser audits run against next start, never next dev (Turbopack watch loop with in-repo artifacts). Next: dual-client sandbox verification at the deployed URL after the founder opens the GitHub/Render gate, then worker + SSE (Task 4 remainder).
- 2026-08-27: Re-polished the landing from live browser runtime measurements of the public TesterArmy demo without copying source or assets. Replaced the supporter matrix with a compliant selected-technology wall, removed the long trademark block and public event/protocol terms, removed the rejected six-feature dossier, built the compact persistent three-stage proceeding, wired valid intake to scroll and launch the local preview, refreshed the current browser-tool contract, and completed desktop, mobile, keyboard, motion, asset, console, and production gates. Next: close the GitHub and Render live gate, then begin the durable backend sequence.
- 2026-08-29: Full engineering review (/plan-eng-review). Deep architecture review agent scanned 15 files for race conditions, security, DRY, and performance. 7 fixes accepted and shipped: (D1) removed dead directPrisma export, freeing 3 Postgres connections; (D2) replaced stampede-prone principalCache.clear() with LRU partial eviction; (D3) fixed X-Forwarded-For rate-limit bypass (read last entry, not first); (D4.1) deduped evidence IDs in citation gate to prevent single-item bypass; (D4.2) sanitized NormalizedIdea fields before re-entering scoring/bench prompts; (D4.3) added inter-stage superseded checks in the pipeline, saving up to 13 wasted LLM calls per stale run; (D4.4) added try/catch and re-entry guard to worker shutdown; (D4.5) narrowed DIMENSION_DEFINITIONS type for compile-time safety. 99 tests pass, TypeScript clean, no regressions. 8 P3 observations logged for post-submission. Stale docs (STATE.md, PLAN.md checkboxes) updated. Next: /plan-ceo-review, then submission prep.
