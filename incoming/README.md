# Incoming Files

This is a temporary landing area for files whose permanent location or dependencies are not yet clear.

Files should not remain here after integration. For each file, inspect it, place it in the appropriate source or documentation directory, reconcile dependencies, and verify the affected behavior.
